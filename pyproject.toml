[project]
name = "website"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pydoc-markdown",
]

[tool.uv.sources]
pydoc-markdown = { path = "pydoc-markdown", editable = true }
